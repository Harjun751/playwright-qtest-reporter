{
	"name": "qtest-playwright-integration",
	"version": "0.1.0",
	"description": "A utility to pass information from playwright test runs to qtest via the qtest management API.",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"test": "vitest run",
		"check": "biome check --write",
		"typecheck": "tsc --noEmit",
		"build": "tsc -p tsconfig.build.json"
	},
	"bin": {
		"qtest-playwright": "./dist/cli/main.js"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.7",
		"@types/node": "^26.1.2",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10"
	},
	"dependencies": {
		"commander": "^15.0.0",
		"fast-xml-parser": "^5.10.1",
		"fast-xml-validator": "^1.4.1",
		"loglevel": "^1.9.2",
		"zod": "^4.4.3"
	}
}
